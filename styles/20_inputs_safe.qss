/* 20_inputs_safe.qss
 * 목적: 입력 위젯 공통(안전판)
 * 금지: QComboBox::down-arrow
 */

QLineEdit, QTextEdit {
    background: #FFFFFF;
    border: 1px solid #D7DCE6;
    border-radius: 8px;
    padding: 4px 8px;
}

QComboBox {
    background: #FFFFFF;
    border: 1px solid #D7DCE6;
    border-radius: 8px;
    padding: 2px 8px;

}

/* Setting 색상 콤보 전용(로컬 스코프) */
QComboBox#ColorCombo {
    padding-right: 28px;
}
QComboBox#ColorCombo::drop-down {
    width: 28px;
}
/* ------------------------------
 * Setting 전용 버튼(국지 스코프)
 * - 전역 QPushButton 금지 원칙 유지
 * ------------------------------ */

/* + 버튼들 */
QPushButton#addCoordButton,
QPushButton#addOuterButton,
QPushButton#addZButton {
    background: #EEF1F6;
    border: 1px solid #D6DBE6;
    border-radius: 8px;
    color: #111827;
    font-weight: 900;
}
QPushButton#addCoordButton:hover,
QPushButton#addOuterButton:hover,
QPushButton#addZButton:hover {
    background: #E6EBF3;
}

/* 이미지 로드/초기화 버튼 */
QPushButton#LoadImageButton,
QPushButton#ResetButton {
    background: #294392;
    border: 1px solid #294392;
    border-radius: 10px;
    color: #FFFFFF;
    font-weight: 800;
    padding: 6px 12px;
}
QPushButton#LoadImageButton:hover,
QPushButton#ResetButton:hover {
    background: #1F347A;
    border-color: #1F347A;
}
QPushButton#LoadImageButton:pressed,
QPushButton#ResetButton:pressed {
    background: #16285C;
    border-color: #16285C;
}
/* ------------------------------
 * MODE 버튼(국지 스코프)
 * - 전역 QPushButton 금지 원칙 유지
 * - objectName=ModePill + property(mode=...) 로만 적용
 * ------------------------------ */
 /* MODE 버튼: QSS 적용 전(네모 기본) 높이에 최대한 가깝게 복구 */

 /* MODE 버튼: 입력칸(QLineEdit)과 동일 규격(높이감) */
QPushButton#ModePill {
    background: #FFFFFF;              /* 입력칸과 동일한 '흰 바탕' */
    border: 1px solid #D7DCE6;
    border-radius: 8px;

    padding: 4px 10px;                /* ✅ 상하 4px로 입력칸과 동일 */
    min-height: 0px;                  /* ✅ 강제 높이 금지 */

    font-weight: 900;
    color: #111827;
    text-align: center;
}

QPushButton#ModePill:hover {
    background: #F4F7FB;
}

QPushButton#ModePill:checked {
    background: #2B4292;
    border: 1px solid #2B4292;
    color: #FFFFFF;
}


QPushButton#ModePill[mode="onepoint"]:checked {
    /* ONE-POINT도 동일한 브랜드 블루로 통일(요구사항) */
    background: #2B4292;
    border: 1px solid #2B4292;
    color: #FFFFFF;
}
/* ---------------------------------------------
 * 프로젝트명 입력칸 vs TopBar 설비명 콤보
 * - 색상 변경 금지 준수: height/패딩만 정합
 * --------------------------------------------- */

/* 프로젝트명 입력칸: 높이 규격 고정 */
QLineEdit#ProjectNameInput {
    min-height: 16px;
    max-height: 16px;
    padding: 4px 10px; /* 기존 감각 유지 */
}

/* 로테이트 왼쪽 설비명 콤보: 동일 높이 + 우측 화살표 공간 확보 */
QComboBox#TopBarMachineCombo {
    min-height: 16px;
    max-height: 16px;
    padding: 4px 28px 4px 10px;  /* top right bottom left */
}

/* 드롭다운 영역 폭만 확보 (▼는 ProxyStyle이 그림) */
QComboBox#TopBarMachineCombo::drop-down {
    border: none;
    width: 22px;
}
/* =================================================
 * CENTER 버튼 기준 pill 규격 (이미 존재)
 * QPushButton#ModePill
 * ================================================= */

/* =================================================
 * PDF 생성 버튼
 * - CENTER 버튼과 크기/모양 완전 동일
 * - 색상만 빨강 계통
 * ================================================= */

QPushButton#PdfButton {
    /* ===== 모양/크기: CENTER 버튼과 동일 ===== */
    border-radius: 8px;
    padding: 4px 10px;
    min-height: 28px;
    max-height: 28px;

    font-weight: 900;
    text-align: center;

    /* ===== 색상만 빨강 ===== */
    background: #B91C1C;
    border: 1px solid #B91C1C;
    color: #FFFFFF;
}

QPushButton#PdfButton:hover {
    background: #991B1B;
    border-color: #991B1B;
}

QPushButton#PdfButton:pressed {
    background: #7F1D1D;
    border-color: #7F1D1D;
}

QComboBox#AnnoStrokeCombo,
QComboBox#AnnoFillCombo,
QComboBox#AnnoArrowCombo,
QComboBox#AnnoTextCombo {
    /* ✅ 크기: 기존 유지 (필요시 숫자만 조정) */
    min-width: 80px;
    max-width: 80px;

    min-height: 20px;
    max-height: 20px;

    border: 1px solid #D7DCE6;
    border-radius: 8px;

    background: #FFFFFF;
    font-weight: 700;

    /* ✅ drop-down(24px) + 여유(10px) = 34px */
    padding: 4px 34px 4px 10px;
}

QComboBox#AnnoStrokeCombo::drop-down,
QComboBox#AnnoFillCombo::drop-down,
QComboBox#AnnoArrowCombo::drop-down,
QComboBox#AnnoTextCombo::drop-down {
    width: 24px;                 /* ✅ PDF(32)처럼 키우지 않음 */
    border-left: 1px solid #D7DCE6;
}

QComboBox#AnnoStrokeCombo::down-arrow,
QComboBox#AnnoFillCombo::down-arrow,
QComboBox#AnnoArrowCombo::down-arrow,
QComboBox#AnnoTextCombo::down-arrow {
    image: url(machining_auto/assets/tools/inverted_triangle_icon.png);
    width: 14px;                 /* ✅ 툴바용 적정 크기 */
    height: 9px;
    margin-top: 1px;
}

QComboBox#AnnoStrokeCombo QAbstractItemView,
QComboBox#AnnoFillCombo   QAbstractItemView,
QComboBox#AnnoArrowCombo  QAbstractItemView,
QComboBox#AnnoTextCombo   QAbstractItemView {
    background: transparent;
    border: none;
    outline: 0;
    border-radius: 8px;
}

QComboBox#AnnoStrokeCombo QAbstractItemView::viewport,
QComboBox#AnnoFillCombo   QAbstractItemView::viewport,
QComboBox#AnnoArrowCombo  QAbstractItemView::viewport,
QComboBox#AnnoTextCombo   QAbstractItemView::viewport {
    background: transparent;
}

QComboBox#AnnoStrokeCombo QAbstractItemView::item,
QComboBox#AnnoFillCombo   QAbstractItemView::item,
QComboBox#AnnoArrowCombo  QAbstractItemView::item,
QComboBox#AnnoTextCombo   QAbstractItemView::item {
    padding: 4px 10px;
    background: transparent;
}

QComboBox#AnnoStrokeCombo QAbstractItemView::item:selected,
QComboBox#AnnoFillCombo   QAbstractItemView::item:selected,
QComboBox#AnnoArrowCombo  QAbstractItemView::item:selected,
QComboBox#AnnoTextCombo   QAbstractItemView::item:selected {
    background-color: #E8EEFF;
}


/* =========================================================
 * INPUTS (ComboBox / SpinBox) — 28px 통일 세트 (최종)
 * - radius: 8px
 * - right panel width: 26px
 * - separator: #D7DCE6
 * - SpinBox arrows: url(...) 강제
 * - SpinBox right panel background: transparent (요청사항)
 * ========================================================= */

/* ---------------------------------------------------------
 * QComboBox (공통)
 * --------------------------------------------------------- */
QComboBox {
    background: #FFFFFF;
    border: 1px solid #D7DCE6;
    border-radius: 8px;

    min-height: 28px;
    max-height: 28px;

    padding-left: 10px;
    padding-right: 30px; /* drop-down 패널 공간 */
}


QComboBox:disabled {
    background: #F5F6FA;
    color: #808080;
    border: 1px solid #E0E4ED;
}

/* 콤보박스 우측 패널 (콤보는 요청 없으므로 기본 배경 유지) */
QComboBox::drop-down {
    width: 26px;
    background: #F2F4F8;
    border-left: 1px solid #D7DCE6;

    border-top-right-radius: 8px;
    border-bottom-right-radius: 8px;
}

QComboBox::drop-down:hover {
    background: rgba(41, 67, 146, 0.10);
}

QComboBox::down-arrow {
    width: 8px;
    height: 8px;
}

/* 콤보 팝업 리스트 */
QComboBox QAbstractItemView {
    background: #FFFFFF;
    border: 1px solid #D7DCE6;
    border-radius: 8px;
    padding: 4px;
    outline: 0;
}

QComboBox QAbstractItemView::item {
    padding: 6px 10px;
}

QComboBox QAbstractItemView::item:selected {
    background: rgba(41, 67, 146, 0.12);
}

/* ---------------------------------------------------------
 * SpinBox (QSpinBox / QDoubleSpinBox) — 공통
 * objectName="AnnoSpinBox" 대상
 * --------------------------------------------------------- */
QAbstractSpinBox#AnnoSpinBox {
    background: #FFFFFF;
    border: 1px solid #D7DCE6;
    border-radius: 8px;

    min-height: 28px;
    max-height: 28px;

    padding-left: 10px;
    padding-right: 30px; /* up/down 패널 공간 */
}

QAbstractSpinBox#AnnoSpinBox QLineEdit {
    background: transparent;
    border: 0px;
    padding: 0px;
}


QAbstractSpinBox#AnnoSpinBox:disabled {
    background: #F5F6FA;
    color: #808080;
    border: 1px solid #E0E4ED;
}

/* ---------------------------------------------------------
 * SpinBox 우측 패널 (배경 투명 + 구분선만 유지)
 * --------------------------------------------------------- */
QAbstractSpinBox#AnnoSpinBox::up-button,
QAbstractSpinBox#AnnoSpinBox::down-button {
    width: 26px;

    /* ✅ 요청사항: 화살표 뒤 배경(우측 패널 배경) 제거 */
    background: transparent;

    /* ✅ 좌측 구분선은 유지 */
    border-left: 1px solid #D7DCE6;
}

/* 🔑 겹침 방지: % 금지, px 고정 */
QAbstractSpinBox#AnnoSpinBox::up-button {
    subcontrol-origin: border;
    subcontrol-position: top right;
    height: 14px;

    border-top-right-radius: 8px;
}

QAbstractSpinBox#AnnoSpinBox::down-button {
    subcontrol-origin: border;
    subcontrol-position: bottom right;
    height: 14px;

    border-bottom-right-radius: 8px;

    /* ✅ 중앙이 비어 보이는 현상 제거: 위/아래 버튼 경계선 */
    border-top: 1px solid #D7DCE6;
}

/* hover 시만 살짝 강조 (배경 제거 요구를 존중하여 hover에서만 표시) */
QAbstractSpinBox#AnnoSpinBox::up-button:hover,
QAbstractSpinBox#AnnoSpinBox::down-button:hover {
    background: rgba(41, 67, 146, 0.08);
}

/* ---------------------------------------------------------
 * SpinBox 화살표 (url 이미지 강제)
 * --------------------------------------------------------- */
QAbstractSpinBox#AnnoSpinBox::up-arrow {
    image: url(machining_auto/assets/tools/triangle_icon.png);
    width: 9px;
    height: 9px;
}

QAbstractSpinBox#AnnoSpinBox::down-arrow {
    image: url(machining_auto/assets/tools/inverted_triangle_icon.png);
    width: 9px;
    height: 9px;
}

/* =========================================================
 * INPUT TEXT ALIGNMENT — 글씨 크기 확대 + 수직 중앙 정렬
 * ========================================================= */

/* -------------------------
 * QComboBox 텍스트
 * ------------------------- */
QComboBox {
    font-size: 14px;          /* ✅ 글씨 약간 키움 */
    line-height: 20px;        /* ✅ 높이 28px 기준 중앙 */
    padding-top: 4px;         /* 위/아래 동일 */
    padding-bottom: 4px;
}

/* 콤보박스 내부 표시 텍스트 */
QComboBox QAbstractItemView {
    font-size: 14px;
}

/* -------------------------
 * SpinBox 텍스트 (내부 QLineEdit)
 * ------------------------- */
QAbstractSpinBox#AnnoSpinBox QLineEdit {
    font-size: 14px;          /* ✅ 콤보와 동일 */
    line-height: 20px;        /* ✅ 수직 중앙 핵심 */
    padding-top: 0px;
    padding-bottom: 0px;
    text-align: center;       /* ✅ 요청: 중앙 정렬 */
}

/* SpinBox 전체도 중앙 정렬 유지 */
QAbstractSpinBox#AnnoSpinBox {
    font-size: 14px;
}

/* =========================================================
 * PdfLayoutCombo — AnnoStrokeCombo 세트 그대로 복제
 * (단, PDF 크기/폭만 유지)
 * ========================================================= */
QComboBox#PdfLayoutCombo {
    /* ✅ PDF 콤보 전용 크기 */
    min-height: 28px;
    max-height: 28px;

    border: 1px solid #D7DCE6;
    border-radius: 8px;

    background: #FFFFFF;
    color: #111827;
    font-weight: 700;

    /* ✅ drop-down(32px) + 여유(10px) = 42px */
    padding: 4px 42px 4px 10px;
}

QComboBox#PdfLayoutCombo::drop-down {
    width: 32px;
    border-left: 1px solid #D7DCE6;

    /* (선택) 우측 패널 라운드가 필요하면 아래 2줄 사용
       border-top-right-radius: 8px;
       border-bottom-right-radius: 8px;
    */
}

QComboBox#PdfLayoutCombo::down-arrow {
    image: url(machining_auto/assets/tools/inverted_triangle_icon.png);
    width: 18px;
    height: 12px;
    margin-top: 1px;  /* 수직 중앙 미세 보정 */
}

/* ✅ 아래 콤보(AnnoStrokeCombo)와 “아이템뷰 처리 방식” 동일 */
QComboBox#PdfLayoutCombo QAbstractItemView {
    background: transparent;
    border: none;
    outline: 0;
    border-radius: 8px;
}

QComboBox#PdfLayoutCombo QAbstractItemView::viewport {
    background: transparent;
}

QComboBox#PdfLayoutCombo QAbstractItemView::item {
    padding: 6px 10px;
    background: transparent;
}

QComboBox#PdfLayoutCombo QAbstractItemView::item:selected {
    background-color: #E8EEFF;
}
